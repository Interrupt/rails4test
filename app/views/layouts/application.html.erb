<!DOCTYPE html>
<html>
<head>
  <title>Railstest</title>
  <link rel="stylesheet" href="/assets/foundation.css">
  <script src="/assets/modernizr.js"></script>
  <link href='http://fonts.googleapis.com/css?family=Montserrat:400,700' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="header"><a href="/">YOURPERKS</a></div>
<div class="notice"><%= notice %></div>
<div class="alert"><%= alert %></div>

<div class="content">
 <div class="row main-row">
   <div class="sidebar large-2 small-2 columns">
      <% @categories.each do |c| %>
      <a href="/partner_category/<%= c.id %>" class="<% if @category && @category.id == c.id %>selected<% end %>"><%= c.name %></a>
      <% end %>
    </div>
    <div class="main large-10 small-10 columns">
      <div class="inner rows">
        <%= yield %>
      </div>
    </div>
 </div>
</div>

</body>
</html>
