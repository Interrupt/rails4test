<h1>Listing perks</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Perk url</th>
      <th>Contact url</th>
      <th>Description</th>
      <th>Rating</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @perks.each do |perk| %>
      <tr>
        <td><%= perk.name %></td>
        <td><%= perk.perk_url %></td>
        <td><%= perk.contact_url %></td>
        <td><%= perk.description %></td>
        <td><%= perk.rating %></td>
        <td><%= perk.status %></td>
        <td><%= link_to 'Show', perk %></td>
        <td><%= link_to 'Edit', edit_perk_path(perk) %></td>
        <td><%= link_to 'Destroy', perk, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Perk', new_perk_path %>
